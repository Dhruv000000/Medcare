# MediCare environment template pointer
# The canonical backend configuration is documented in backend/.env.example.
# Create backend/.env locally on the Windows development machine and never commit it.
