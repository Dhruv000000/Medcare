Phase 24 newly executed smoke checkpoint: Patient A (`phase24.patient@example.test`) logged in through the preserved login UI. The medical-record page loaded one authorized synthetic record (`Synthetic CBC`) with `Phase24 Doctor`; the page used the new safe DOM renderer and displayed the record details control. The protected attachment was seeded as a synthetic PDF and the record detail modal exposed the protected download control.
Patient file smoke checkpoint: opening the record modal showed `phase24-record.pdf`, validated `application/pdf`, 32 bytes, and an enabled `Download` control. Clicking it completed the authenticated protected-download flow. The reports page then loaded one authorized synthetic `Synthetic Blood Report` and exposed the dedicated `Download Report` control in the existing modal structure.
Session checkpoint: Patient A’s authenticated report API request returned HTTP 200, and the existing CSRF-protected logout request returned HTTP 200. The browser was then redirected to the login page for Patient B testing.
Cross-patient smoke checkpoint: Patient B (`phase24.other.patient@example.test`) logged in successfully. The medical-record page rendered only `Other synthetic record` for `Other Patient`; Patient A’s `Synthetic CBC` was absent from the page and no Patient A file metadata was exposed.
Patient B API boundary checkpoint: authenticated direct requests to Patient A’s record-download and report-download paths returned HTTP 404 with controlled object-not-found responses. Direct CSRF-bearing POST requests to the patient medical-record and report collection routes returned HTTP 405, confirming patient clinical writes remain denied.
Authorized-doctor smoke checkpoint: `phase24.doctor@example.test` logged in through the Doctor role. The dashboard listed only the authorized synthetic Phase24 Patient appointment. Clicking the existing patient View control opened the new appointment-scoped clinical viewer, which rendered `Synthetic CBC` and `Synthetic Blood Report`, safe attachment metadata, and protected download controls for both files.
Authorized-doctor download checkpoint: the doctor viewer’s record download control completed the protected request, and direct authorized API requests for both record and report downloads returned HTTP 200 with `application/pdf`. The server-side response tests already verify `X-Content-Type-Options: nosniff`; browser fetch cannot expose that custom header without CORS header exposure. The first browser response revealed that MedicalReportSerializer omitted content type/size metadata; this was corrected, and the focused Phase 24 plus clinical regression tests passed again (20 tests).
After restarting the API, the authorized doctor report endpoint returned HTTP 200 with `phase24-report.pdf`, `application/pdf`, and 32-byte metadata. The authorized doctor logout returned HTTP 200 and the post-logout session check returned HTTP 403.
Final cross-doctor smoke checkpoint: `phase24.other.doctor@example.test` logged in as Doctor. The dashboard returned only `Other Patient` (patient_id 2); the direct doctor medical-record list filtered to Patient A (patient_id 1) returned `[]`. Direct requests to Patient A’s known record and report download paths returned HTTP 404 with controlled `{"detail":"File not found."}` responses. This completes the unrelated-doctor cross-authorization denial check.
Admin behavior smoke checkpoint: `phase24.admin@example.test` logged in through the preserved Admin role and reached the existing Admin Dashboard. The authenticated Admin session was denied access to both patient and doctor clinical-file download routes with HTTP 403, while `/api/auth/me/` returned HTTP 200. This preserves the existing Admin authorization model and does not grant unjustified clinical-file access.
