Phase 25 synthetic browser smoke — authorized doctor: `phase24.doctor@example.test` logged in through the preserved Doctor role, opened the existing Academic AI workflow, submitted the valid synthetic 13-feature payload, and received the existing model result (`label_absent`, model probability `0.16164121253810007`, model `uci-heart-disease-logreg-v1.0.0`). The new `View Reports` control loaded one authorized report from `/api/ai/reports/`; the report rendered the model version, preprocessing version, label, probability, disclaimer, clinician-responsibility wording, and 13 model-tied contributions through safe DOM construction. Direct inspection confirmed the persisted explanation contained no `value` fields or patient identifiers.
Phase 25 synthetic browser smoke — patient: `phase24.patient@example.test` logged in through the preserved Patient role. The existing Patient AI Health Insights page displayed its limited-access message stating that no prediction request was sent. Direct authenticated requests returned HTTP 403 for `/api/ai/heart-risk/predict/` and `/api/ai/reports/`, while the existing patient clinical-record endpoint returned HTTP 200. No prediction history was exposed.
Phase 25 synthetic browser smoke — administrator: `phase24.admin@example.test` logged in through the preserved Admin role and reached the existing Admin Dashboard. `/api/admin/ai-audit/summary/` returned HTTP 200 with only `total_events`, `completed_events`, `rejected_events`, and the fixed model version. The summary contained no `prediction_label` or `patient_id`. `/api/ai/reports/` returned HTTP 403 and a known detailed report URL returned HTTP 404.
Phase 25 synthetic browser smoke — logout: administrator logout returned HTTP 200. A subsequent request to the previously known protected doctor report URL returned HTTP 403 with `Authentication credentials were not provided.`
Phase 25 synthetic browser smoke — unrelated doctor: `phase24.other.doctor@example.test` logged in through the Doctor role and the dashboard listed only `Other Patient`. The protected report list returned HTTP 200 with `[]`; a known report identifier belonging to the first doctor returned HTTP 404 with `Report not found.`. No cross-doctor report disclosure occurred.
