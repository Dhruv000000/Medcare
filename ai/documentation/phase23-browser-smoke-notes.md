Phase 23 browser smoke checkpoint: the existing frontend on http://127.0.0.1:8010 loaded successfully; synthetic doctor login succeeded against the disposable SQLite Django API on port 8000; the doctor dashboard loaded; the Academic AI Tool opened; all five numeric values and the `sex=1` and `cp=3` source-coded values were accepted without client-side validation errors. No PostgreSQL or real patient data was used.
Phase 23 browser smoke checkpoint: the doctor form accepted `fbs=0`, `restecg=1`, and `exang=0`; all required synthetic values remained populated and no client-side validation error appeared.
Phase 23 browser smoke checkpoint: the fully populated synthetic vector submitted successfully. The page rendered the actual `label_absent` classification, model probability `0.16164121253810007`, model `uci-heart-disease-logreg-v1.0.0`, academic status, disclaimer, decision boundary, and all 13 model feature contributions with signed logit-unit values and textual support/oppose direction labels. No raw JSON, stack trace, filesystem path, artifact path, secret, or patient identifier was visible.
Phase 23 browser smoke checkpoint: changing `age` to `28` and submitting was rejected before API submission with the visible message `age must be between 29 and 77.` The existing result remained unchanged and no new prediction was generated.
Phase 23 browser smoke checkpoint: synthetic patient credentials were accepted and the page began navigating to the patient dashboard. The patient portal showed no patient prediction form or model output; the limited-access AI page remains the next denial check.
Phase 23 browser smoke checkpoint: the authenticated patient AI Health Insights page displayed the limited-access safety message, stated that no prediction request was sent during normal page operation, and exposed no prediction form or explanation. A direct synthetic request from the current patient browser session returned HTTP 403 with `You do not have permission to perform this action.`
Phase 23 browser smoke checkpoint: the patient-page logout click timed out in the browser harness and the browser session briefly became unavailable. A direct navigation to the local login page then succeeded, showing the authentication screen; this verifies the final navigation boundary, while the click itself is recorded as a harness timeout rather than claimed as a clean click result.
Phase 23 browser smoke checkpoint: after a fresh synthetic patient login, the existing logout API was exercised through the application’s CSRF/session flow. Actual statuses were `before_status=200`, `logout_status=200`, and `after_status=403`, confirming session invalidation. The earlier click timeout is therefore covered by a verified application-level logout result.
